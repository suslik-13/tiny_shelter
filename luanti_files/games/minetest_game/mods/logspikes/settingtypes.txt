# The size that log textures will be scaled to.
logspikes.texture_size (Texture size) enum 256 16,32,64,128,256,512,1024,2048,4096,8192

# The amount of damage that spikes deal to players and entities every second.
logspikes.damage_per_second (Damage per second) int 4 0 1000
